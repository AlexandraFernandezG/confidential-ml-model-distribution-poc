# Template only - do NOT apply this file as-is and do NOT commit real key
# material to git.
#
# In practice the Secret is created imperatively by scripts/create-secret.sh
# (the Control Plane/CI step) directly from the encryption.key /
# encryption.nonce files produced by producer/src/select_and_encrypt.py:
#
#   kubectl create secret generic model-decryption-key \
#     --namespace confidential-ml-poc \
#     --from-file=encryption.key=out/encryption.key \
#     --from-file=encryption.nonce=out/encryption.nonce
#
# This declarative form is kept here only as a reference for what that
# command produces (values below are placeholders, base64 of literal
# "REPLACE_ME").
apiVersion: v1
kind: Secret
metadata:
  name: model-decryption-key
  namespace: confidential-ml-poc
type: Opaque
data:
  encryption.key: UkVQTEFDRV9NRQ==
  encryption.nonce: UkVQTEFDRV9NRQ==
